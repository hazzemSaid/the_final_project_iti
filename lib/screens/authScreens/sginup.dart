import 'package:flutter/material.dart';

class SginUp extends StatelessWidget {
  const SginUp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('sginup'),
      ),
    );
  }
}
