import 'package:flutter/material.dart';

class SginIn extends StatelessWidget {
  const SginIn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('sginin'),
      ),
    );
  }
}
